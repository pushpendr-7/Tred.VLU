services:
  - type: web
    name: auction_site
    env: python
    plan: free
    pythonVersion: "3.13.4"
    buildCommand: "pip install -r requirements.txt && python manage.py collectstatic --noinput"
    startCommand: "gunicorn auction_site.wsgi:application"
    envVars:
      - key: SECRET_KEY
        generateValue: true
      - key: DEBUG
        value: "False"







    